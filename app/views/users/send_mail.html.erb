<title>Mail Reports | iLead Cricket Auction</title>
<style>
    body{
    background: linear-gradient(to top, #f58754, #1a237e);
	background-color: #1a237e;
      background-size: cover; /* Ensures the background covers the entire viewport */
      background-attachment: fixed;
	}
    .team-container {
     /* display: flex; */
     display: flex;
        justify-content: center;
     /* justify-content: space-between; */
    /* flex-wrap: wrap; */
  }
    
    .team-table {
    background: linear-gradient(to right, #f57224, #f58754);
    font-size: 1.2em;
    color: black;
    width: 70%;
    margin-top: 20px;
    margin-bottom: 20px;
    border-collapse: collapse;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
  }

  .team-table th, .team-table td {
    border: 2px solid #ddd;
    padding: 12px;
    text-align: center;
  }

  .team-name {
    background-color: #f2f2f2;
    cursor: pointer;
  }

  .team-table th {
    color: #fff;
    background-color: #1a237e;
  }
  .pagination {
      margin-top: 20px;
      list-style-type: none;
      display: flex;  
      justify-content: center;
    }
  .pagination li {
    margin: 0 5px;
  }
  .pagination a {
    text-decoration: none;
    padding: 8px 12px;
    background-color: #f57224;
    color: white;
    border-radius: 4px;
  }
  .pagination .active a {
    background-color: #1a237e;
  }
  h5{
      text-align: center;
  }
</style>
<body>
  <% i = 0 %>
  <h1 style="text-align: center; color:#ddd">Mail Reports Sent to: ( <%= @sent_count %> / <%= @total_count %>)</h1>
  <div class="team-container">
    <table class="team-table">
      <thead>
      <tr>
        <th>S No.</th>
        <th style="padding-right: 50px; padding-left: 20px;">Player Name</th>
        <th>Action</th>
        <th>Status</th>
      </tr>
      </thead>
      <tbody>
        <% @users.each do |user| %>
          <tr>
          <td><%=i = i+1 %></td>
          <td style="padding-right: 50px; padding-left: 20px;"><%= user.full_name %></td>
          <td style="padding-right: 100px;"><%= button_to 'Mail Report', player_mail_user_path(user), method: :post, style: "color: #f57224;" %></td>
          <td style="padding-right: 100px;">
            <%= user&.interested_commentary == "sent" ? "sent #{user.count} time" : "not sent yet" %> | <%if user&.team&.present?%>
              Sold
            <%else%>
              Unsold
            <%end%>
          </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</body>

  