<!DOCTYPE html>
<html>
<head>
  <title>iLead Cricket Auction</title>
  <style type="text/css">
    body {
      background: linear-gradient(to top, #f58754, #1a237e);
      background-color: #1a237e;
      background-size: cover; /* Ensures the background covers the entire viewport */
      background-attachment: fixed;
      color: black;
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 800px;
      margin: 20px auto;
      background-color: #fff;
      padding: 20px;
      border: 2px solid #ddd;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    }
    h2 {
      text-align: center;
      color: #f58754;
    }
    .user-details p {
      margin-bottom: 10px;
      font-size: 18px;
    }
    .actions {
      text-align: center;
      margin-top: 20px;
    }
    .actions a {
      display: inline-block;
      padding: 10px 20px;
      background-color: #f57224;
      color: white;
      border-radius: 4px;
      text-decoration: none;
      margin: 10px;
      transition: background-color 0.3s, color 0.3s;
    }
    .actions a:hover {
      background-color: #1a237e;
    }
  </style>
</head>
<body>
  <div class="container" style="margin-top: 200px;">
    <%if @user.team.nil?%>
        <h2><%= @user.full_name%> | Yet to be auctioned </h2>
    <%else%>    
        <h2><%= @user.full_name%> | Sold for ₹<%= @user&.price %> to <%= @user&.team %> </h2>
    <%end%>
    

    <div class="user-details">
      <p><strong>ID:</strong> <%= @user.id %></p>
      <p><strong>Full Name:</strong> <%= @user.full_name %></p>
      <p><strong>Email:</strong> <%= @user.email %></p>
      <p><strong>Team:</strong> <%= (@user&.team.nil?)? "Yet to be auctioned" : @user&.team %></p>
      <p><strong>Price(₹):</strong> <%= (@user&.price==0)? "Yet to be auctioned" : @user.price  %></p>
      <p><strong>Skills:</strong> <%= @user&.skills %></p>
      <p><strong>Level:</strong> <%= @user&.level_of_skill %></p>
    
    </div>

    <div class="actions">
      <%#= link_to 'Edit', edit_admin_user_path(@user) %>
      <%= link_to 'Back', admin_users_path %>
    </div>
  </div>
</body>
</html>
