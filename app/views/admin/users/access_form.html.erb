<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TheCricAuction - Home</title>
  <link rel="icon" type="image/x-icon" href="assets/images/applogo.png">
  <style>
    /* Global styles */
    body {
      background: linear-gradient(to top, #f58754, #1a237e);
      background-color: #1a237e;
      background-size: cover; /* Ensures the background covers the entire viewport */
      background-attachment: fixed;
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      flex-direction: column;
    }
    
  </style>
  <body>
    <h1>Access</h1> 
    <%= form_for access_form, url: admin_access_form_path, method: :post, multipart: true do |form| %>
    <%#= form_with(model: [@access_form], local: true) do |form| %> 
        <div class="field">
            <%= form.label :full_name %>
            <%= form.text_field :full_name, required: true %>
        </div>

        <div class="field">
            <%= form.label :email %>
            <%= form.email_field :email, required: true %>
        </div>

        <div class="field">
            <%= form.label :description %>
            <%#= form.text_field :description, class: 'form-control' %>
            <%= form.text_field :description, required: true %>
        </div>
    <%end%>
  </body>